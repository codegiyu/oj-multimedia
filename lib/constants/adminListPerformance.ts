/** Target P95 latency for admin list API responses (ms). */
export const ADMIN_LIST_API_P95_TARGET_MS = 300;

/** Debounced search delay used by FilterableDataPage (ms). */
export const ADMIN_LIST_SEARCH_DEBOUNCE_MS = 350;

/** Max iterations for synchronous query-builder perf smoke in CI. */
export const ADMIN_LIST_QUERY_BUILD_ITERATIONS = 2000;

/** Max wall time for query-builder perf smoke (ms). */
export const ADMIN_LIST_QUERY_BUILD_MAX_MS = 50;
