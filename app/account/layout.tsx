import { AccountAuthIsland } from '@/components/layout/AccountAuthIsland';

export default function AccountLayout({ children }: { children: React.ReactNode }) {
  return <AccountAuthIsland>{children}</AccountAuthIsland>;
}
